export const LOCAL_STORAGE_AUTH_TOKEN_KEY = 'authToken';

export const NOTIFICATION_OUTRO_ANIMATION_DURATION_MS = 200;
export const NOTIFICATION_INTRO_ANIMATION_DURATION_MS = 100;
export const NOTIFICATIONS_HIDE_DELAY_MS = 6000;

export const USERNAME_REGEX = /^[A-Za-z0-9.]+$/;
export const PASSWORD_REGEX = /^[A-Za-z0-9!@#$%^&*.,/\\]+$/;
